{
  "name": "bangladesh-election-poll",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend",
    "lint": "npm run lint -w frontend",
    "constituencies:build": "npm run constituencies:build -w frontend",
    "candidates:build": "npm run candidates:build -w frontend"
  }
}
